{
  "name": "superliga-predictor-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node ../scripts/check-syntax.mjs src",
    "test": "node --test test/*.test.mjs",
    "build": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.118.0"
  }
}
