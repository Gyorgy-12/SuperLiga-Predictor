{
  "name": "superliga-predictor-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "check": "node scripts/check-syntax.mjs src --order src/app.order.txt",
    "test": "npm run check && npm --prefix superliga-worker run check"
  }
}
